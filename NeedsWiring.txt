============================================================
SANDLOT PICKS — SERVER-SIDE WIRING REQUIREMENTS
============================================================

This document describes the frontend features that are currently
running on mock data / localStorage and need real backend support.
Each section is self-contained — copy any one section into a
server-side chat to scope and build that feature in isolation.

Existing API conventions (already in use by the app):
  - Base URL prefix: /api/v1
  - Auth: JWT Bearer token in Authorization header
  - Error responses: { "detail": "<human-readable string>" }
    Frontend reads err.message (mapped from response.detail)
  - User profile lives at GET /api/v1/users/me
  - Subscriptions namespace: /api/v1/subscriptions/...
  - Admin namespace: /api/v1/admin/...

Project: React 18 + Create React App, Supabase as content DB,
Stripe for billing. Server is Python/FastAPI based on existing
endpoint shape.


============================================================
FEATURE 1 — ALERTS / NOTIFICATIONS SYSTEM
============================================================

WHAT IT IS
----------
Admins can publish alerts that either (a) pop up as a modal the
next time the targeted user signs in, or (b) silently land in
the user's inbox on their settings page. Alerts can be sent to
all users or to a specific list of email addresses.

CURRENT STATE
-------------
All alert data lives in browser localStorage under keys
`spa_admin_alerts_v1` and `spa_admin_alerts_read_v1`. This means
targeted-email alerts don't actually reach the right user since
the data never crosses browsers. Needs a real backend.

FRONTEND FILES THAT WILL CONSUME THIS API
-----------------------------------------
- src/data/constants/alertsMockData.js   (swap helpers for fetch calls)
- src/components/AlertSignInModal.jsx     (auto-pops on login)
- src/components/AlertInbox.jsx           (settings page inbox card)
- src/components/pages/Admin/AlertComposerModal.jsx (admin composer)

DATA MODEL
----------
Table: alerts
  id              uuid PRIMARY KEY DEFAULT gen_random_uuid()
  subject         text NOT NULL
  body            text NOT NULL
  display_type    text NOT NULL CHECK (display_type IN ('modal', 'general'))
  target_type     text NOT NULL CHECK (target_type IN ('all', 'specific'))
  target_emails   text[] NOT NULL DEFAULT '{}'
                  -- only populated when target_type = 'specific'
  created_at      timestamptz NOT NULL DEFAULT now()
  created_by      uuid REFERENCES users(id)
  expires_at      timestamptz NULL  -- optional; treat NULL as "no expiry"

Table: alert_reads
  alert_id        uuid REFERENCES alerts(id) ON DELETE CASCADE
  user_id         uuid REFERENCES users(id)  ON DELETE CASCADE
  read_at         timestamptz NOT NULL DEFAULT now()
  PRIMARY KEY (alert_id, user_id)

Index: alert_reads(user_id) for fast per-user lookups.

ENDPOINTS
---------

(1) POST /api/v1/admin/alerts
    Auth: admin only (403 otherwise)
    Body:
      {
        "subject": "Brief outage tonight",
        "body": "We're pushing a deploy at 11pm ET. ~30 min downtime.",
        "display_type": "modal",                  // 'modal' | 'general'
        "target_type": "all",                     // 'all'   | 'specific'
        "target_emails": ["a@b.com", "c@d.com"]   // empty array when target_type='all'
      }
    Validation:
      - subject and body required, both non-empty
      - if target_type = 'specific', target_emails must have ≥1 valid email
    Response 201:
      {
        "id": "uuid",
        "subject": "...",
        "body": "...",
        "display_type": "modal",
        "target_type": "all",
        "target_emails": [],
        "created_at": "2026-04-28T15:00:00Z",
        "created_by": "uuid"
      }
    Errors:
      400 { "detail": "Subject is required." }
      400 { "detail": "At least one recipient email is required." }
      403 { "detail": "Admin access required." }

(2) GET /api/v1/admin/alerts
    Auth: admin only
    Returns: array of all alerts (full objects, newest first), most recent 100.
    Used for: future admin "manage existing alerts" view.

(3) DELETE /api/v1/admin/alerts/:id
    Auth: admin only
    Returns: 204 No Content on success
    Errors: 404 { "detail": "Alert not found." }

(4) GET /api/v1/users/me/alerts
    Auth: any authenticated user
    Server-side filtering — NEVER return alerts not targeted at the user.
    SQL pattern:
      SELECT a.*, ar.read_at IS NOT NULL AS is_read
      FROM alerts a
      LEFT JOIN alert_reads ar ON ar.alert_id = a.id AND ar.user_id = :user_id
      WHERE (a.target_type = 'all'
             OR :user_email = ANY(a.target_emails))
        AND (a.expires_at IS NULL OR a.expires_at > now())
      ORDER BY a.created_at DESC
    Response:
      [
        {
          "id": "uuid",
          "subject": "...",
          "body": "...",
          "display_type": "modal",
          "created_at": "2026-04-25T16:30:00Z",
          "is_read": false
        },
        ...
      ]
    Note: do NOT return target_type or target_emails to non-admin users.
    The is_read flag drives the unread badge + sign-in modal queue.

(5) POST /api/v1/users/me/alerts/:id/read
    Auth: any authenticated user
    Marks a single alert as read by the current user.
    Response: 204 No Content
    Idempotent — second call is a no-op.
    Errors: 404 if alert not visible to this user (don't leak existence).

CLIENT BEHAVIOR TO BE AWARE OF
------------------------------
- On every successful login, the client immediately calls (4) and
  filters for `display_type === 'modal' && !is_read`. Those pop as
  a queued modal sequence; user clicks "Got it" → call (5).
- The settings inbox calls (4) on mount and re-renders unread badge
  off the `is_read` flag.

OPEN QUESTIONS
--------------
- Soft delete for compliance? (recommended: add `deleted_at`)
- Rate limit (5)? Probably not needed — but add basic auth-protection.


============================================================
FEATURE 1A — ALERT READ RECEIPTS (ADMIN VIEW)
============================================================

WHAT IT IS
----------
Admins want to see who has acknowledged each modal alert (clicked
"Got it" on the sign-in modal). This drives two UI states in the
Alert Manager section of the admin dashboard:

  1. target_type = 'all'      → show "{N} of {audience_size} confirmed"
  2. target_type = 'specific' → show per-recipient list: which targeted
                                emails have clicked "Got it" and when

Inbox / "general" alerts are out of scope here — read_at on those is
just an "opened" signal, not an explicit acknowledgement.

CURRENT STATE
-------------
The alert_reads table already exists (created in the original Feature
1 server build) and is populated by POST /api/v1/users/me/alerts/{id}/read
when a user dismisses the sign-in modal. The data is captured but no
admin endpoint surfaces it. The Alert Manager UI at
src/components/pages/Admin/AdminPage.jsx has no read-receipt display
today.

FRONTEND FILES THAT WILL CONSUME THIS API
-----------------------------------------
- src/components/pages/Admin/AdminPage.jsx
    (admin-alert-row renders the receipt summary inline + an
     expandable "Reads" drawer or button for the per-email detail)
- src/data/services/alertsService.js
    (add getAlertReads(alertId) helper)

DATA — NO SCHEMA CHANGES NEEDED
-------------------------------
Everything is derivable from the existing tables:
  - alerts                (target_type, target_emails, display_type)
  - alert_reads           (alert_id, user_id, read_at)
  - users                 (id, email)
For target_type='all' the "audience size" should match the same filter
the user-facing GET /api/v1/users/me/alerts uses — i.e. all users for
whom this alert would have been visible at the time of querying. A
reasonable v1 simplification: count = total active users (excluding
soft-deleted). Document whichever you choose.

ENDPOINTS
---------

(1) Extend GET /api/v1/admin/alerts response
    Add two aggregate fields to each alert row so the list view can
    render the headline number without N round-trips:
      {
        "id": 42,
        "subject": "...",
        ...,
        "read_count": 12,            // unique users in alert_reads
        "audience_size": 287         // see notes below
      }
    audience_size semantics:
      - target_type = 'all'      → count of users targetable today
      - target_type = 'specific' → length of target_emails
    audience_size MAY be null if computing it is expensive; the
    frontend will fall back to showing just "{read_count} confirmed".

(2) GET /api/v1/admin/alerts/{alert_id}/reads
    Auth: admin only (403 otherwise)
    Returns the per-recipient detail.

    For target_type = 'specific':
      [
        { "email": "a@b.com", "user_id": 12,   "read_at": "2026-05-14T18:22:00Z" },
        { "email": "c@d.com", "user_id": null, "read_at": null },
        { "email": "e@f.com", "user_id": 17,   "read_at": null }
      ]
      - One entry per address in target_emails
      - user_id = null if the email doesn't match a registered user
      - read_at = null if they haven't clicked "Got it"
      - Sort: read first (newest first), then unread alphabetical

    For target_type = 'all':
      {
        "read_count": 12,
        "audience_size": 287,
        "reads": [
          { "email": "a@b.com", "user_id": 12, "read_at": "2026-05-14T18:22:00Z" },
          ...
        ]
      }
      - "reads" is paginated/capped to the most-recent 200 to avoid
        dumping the entire user table in one response. The headline
        number (read_count) is the source of truth.

    Errors:
      403 { "detail": "Admin access required." }
      404 { "detail": "Alert not found." }

CLIENT BEHAVIOR
---------------
- The admin row already shows audience pill + type chip. For modal
  alerts it will add a small "{read_count} of {audience_size} confirmed"
  badge inline. Non-modal rows skip this entirely.
- Clicking the row (or a "View reads" affordance) opens a drawer that
  calls (2) and renders the per-recipient table.
- Real-time updates are NOT required for v1 — receipts refresh when
  the admin reopens the drawer or refreshes the page.

OPEN QUESTIONS
--------------
- Should deleting an alert (DELETE /api/v1/admin/alerts/{id}) also
  remove the read receipts? Today the CASCADE on alert_reads says yes.
  If admins ever want post-hoc audit, switch to soft-delete first
  (see Feature 1 open question on `deleted_at`).
- Privacy: per-recipient read data exposes user activity. Document
  the admin-only access; consider adding it to a future audit log.


============================================================
FEATURE 2 — EMAIL CAMPAIGNS
============================================================

WHAT IT IS
----------
Admin composes an email (subject, body, optional CTA), picks an
audience segment (all / free / premium / weekly / monthly / annual /
lapsed), and sends. The frontend shows a recipient count, supports
test sends to the admin's own email, and displays a history of
past campaigns.

CURRENT STATE
-------------
Frontend is fully built — composer, audience picker, live preview,
test-send button, confirmation modal, recent-campaigns history.
All using mock counts and `MOCK_CAMPAIGN_HISTORY` from
src/data/constants/campaignsMockData.js. No emails actually sent.

FRONTEND FILES THAT WILL CONSUME THIS API
-----------------------------------------
- src/components/pages/Admin/CampaignsPage.jsx
- src/components/pages/Admin/AdminPage.jsx       (recent campaigns row)
- src/data/constants/campaignsMockData.js        (will be replaced)

EMAIL PROVIDER RECOMMENDATION
-----------------------------
Use Resend (https://resend.com). Free tier covers low volume,
modern API, simple webhooks. Postmark, SendGrid, AWS SES also fine.

DATA MODEL
----------
Table: campaigns
  id              uuid PRIMARY KEY DEFAULT gen_random_uuid()
  subject         text NOT NULL
  body            text NOT NULL
  cta_label       text NULL
  cta_url         text NULL
  audience        text NOT NULL
                  CHECK (audience IN ('all','free','premium','weekly',
                                      'monthly','annual','lapsed'))
  recipient_count int NOT NULL DEFAULT 0  -- snapshotted at send time
  status          text NOT NULL DEFAULT 'sent'
                  CHECK (status IN ('sent','failed','draft'))
  sent_at         timestamptz NULL
  created_at      timestamptz NOT NULL DEFAULT now()
  created_by      uuid REFERENCES users(id)

Optional: campaign_recipients table if you want per-recipient
tracking (open/click/bounce) — Resend stores this on their side,
so for v1 you can skip it.

AUDIENCE RESOLUTION
-------------------
Map each `audience` key to a SQL filter:
  all      → users WHERE email_updates = TRUE
  free     → users WHERE email_updates = TRUE AND subscription_tier = 'free'
  premium  → users WHERE email_updates = TRUE AND subscription_tier = 'premium'
  weekly   → users WHERE email_updates = TRUE AND subscription_plan  = 'weekly'
  monthly  → users WHERE email_updates = TRUE AND subscription_plan  = 'monthly'
  annual   → users WHERE email_updates = TRUE AND subscription_plan  = 'annual'
  lapsed   → users WHERE email_updates = TRUE
                    AND subscription_status IN ('canceled','cancelled')

ALWAYS exclude users with `email_updates = FALSE` regardless of
audience selection. This is a hard CAN-SPAM/GDPR requirement.

ENDPOINTS
---------

(1) GET /api/v1/admin/campaigns
    Auth: admin only
    Returns: [{ id, subject, audience, recipient_count, sent_at, status }, ...]
    Sorted by sent_at DESC. Last 50 entries.
    Used by: AdminPage Campaign Manager row + CampaignsPage history.

(2) GET /api/v1/admin/campaigns/audience-counts
    Auth: admin only
    Returns:
      {
        "all":     412,
        "free":    287,
        "premium": 98,
        "weekly":  22,
        "monthly": 64,
        "annual":  12,
        "lapsed":  27
      }
    Used to populate the audience picker live counts.

(3) POST /api/v1/admin/campaigns/test
    Auth: admin only
    Body:
      {
        "subject": "...",
        "body": "...",
        "cta_label": "View Predictions",  // nullable
        "cta_url": "https://...",          // nullable
        "to_email": "admin@sandlotpicks.com"
      }
    Sends a single email to to_email. Does NOT create a campaign row.
    Response 200: { "ok": true }
    Errors:
      400 { "detail": "Subject is required." }
      502 { "detail": "Email provider unavailable. Try again." }

(4) POST /api/v1/admin/campaigns/send
    Auth: admin only
    Body:
      {
        "subject": "...",
        "body": "...",
        "cta_label": "View Predictions",
        "cta_url": "https://...",
        "audience": "free"
      }
    Behavior:
      a. Resolve audience to recipient list (filter by email_updates).
      b. Insert campaigns row with status='sent', sent_at=now(),
         recipient_count = len(recipients).
      c. Send to provider in batched calls (Resend: up to 100 per call).
      d. Use BCC or per-message send — never put all addresses in To.
      e. Append unsubscribe link automatically:
         https://www.sandlotpicks.com/unsubscribe?token=<jwt-or-uuid>
    Response 201:
      {
        "id": "uuid",
        "recipient_count": 287,
        "sent_at": "2026-04-28T15:00:00Z",
        "status": "sent"
      }
    Errors:
      400 { "detail": "Audience has no recipients." }
      400 { "detail": "Subject and body are required." }
      502 { "detail": "Email provider error: <provider message>" }

EMAIL TEMPLATE
--------------
Server should wrap user-supplied body in a branded HTML template:
  - Header: "⚾ Sandlot Picks" wordmark
  - Body: rendered with line breaks as <p> tags
  - CTA button (if provided): teal gradient, full-width, links to cta_url
  - Footer: small grey text with manage-preferences and unsubscribe links
The frontend preview at CampaignsPage.jsx uses this exact structure
as a reference — replicate it server-side.

UNSUBSCRIBE FLOW
----------------
Already exists at /unsubscribe in the frontend. Backend should
accept a one-click unsubscribe token from the email and set
users.email_updates = FALSE.


============================================================
FEATURE 3 — AI ASSIST (LLM PROXY)
============================================================

WHAT IT IS
----------
The article editor and campaign composer have ✨ AI Assist
buttons that should call an LLM (Claude or GPT) to generate or
refine content. The frontend calls a single proxy endpoint —
the server holds the API key and tracks usage.

CURRENT STATE
-------------
Frontend uses pre-canned mock generators in:
  - src/components/pages/Admin/CampaignsPage.jsx
      → mockGenerateBody, mockSubjectIdeas
  - src/components/pages/Admin/ArticleEditor.jsx
      → mockTitleIdeas, mockSummaryDraft, mockBlockRefine

Each function returns hardcoded sample text after a 1.1s setTimeout.

WHY A PROXY (instead of frontend calling LLM directly)
------------------------------------------------------
- API keys must never live in the browser bundle.
- Lets you rate-limit per admin user.
- Lets you log usage / cost per admin.
- Lets you swap providers without redeploying frontend.

ENDPOINT
--------
POST /api/v1/admin/ai/generate
Auth: admin only
Body:
  {
    "task": "campaign_subject" | "campaign_body" |
            "article_title"    | "article_summary" |
            "article_block_refine",
    "context": {
      // task-specific fields below
    }
  }

Task-specific contexts:

  campaign_subject:
    { "audience": "free", "count": 5 }
    → returns array of 5 subject line strings

  campaign_body:
    {
      "audience": "premium",
      "current_body": "<existing draft or empty>",
      "instruction": "Announce that pitcher props are live"
    }
    → returns a single markdown body string

  article_title:
    {
      "summary": "<post.summary>",
      "content_text": "<flat-text version of all blocks>",
      "type": "article" | "blog",
      "count": 5
    }
    → returns array of 5 title strings

  article_summary:
    { "content_text": "<flat-text>" }
    → returns a single summary string

  article_block_refine:
    {
      "block_text": "<original text>",
      "block_type": "paragraph" | "heading" | "subheading" | "quote",
      "action": "improve" | "shorter" | "expand" | "casual" | "sharper",
      "instruction": "<optional custom prompt>"
    }
    → returns a single refined text string

Response shapes:
  Single string:  { "text": "..." }
  List of strings: { "texts": ["...", "...", "..."] }

Errors:
  400 { "detail": "Unknown task: <task>" }
  429 { "detail": "AI usage limit reached. Try again in 1 hour." }
  502 { "detail": "AI provider error." }

PROVIDER RECOMMENDATION
-----------------------
Anthropic Claude (claude-haiku-4-5 for speed/cost on the refine
tasks; claude-sonnet-4-6 for higher-quality summary/title work).
Pass the existing brand voice in a system prompt — punchy, data-
driven, baseball-fluent, no hype.

USAGE TRACKING (optional v1)
----------------------------
Log every call to ai_usage_log table:
  user_id, task, prompt_tokens, completion_tokens, cost_usd,
  created_at. Lets you see who's spending what over time.


============================================================
FEATURE 4 — FREE WEEK PROMO ENFORCEMENT (FREE7)
============================================================

WHAT IT IS
----------
Free-tier users can claim one free week of premium access by
redeeming the FREE7 promo code on the Settings page. The flow
calls POST /api/v1/subscriptions/redeem-promo, redirects through
Stripe, and the user lands on /subscription/success as premium.

CURRENT STATE
-------------
The redemption flow itself is wired (you said you implemented
the endpoint server-side already). The remaining piece is making
sure each user can only claim ONCE.

WHAT THE CLIENT NEEDS
---------------------
The /api/v1/users/me response should include a new field:

  free_week_claimed_at: timestamptz | null

Frontend uses this to:
  - Hide the Free Week card on Settings once it's been claimed
  - Show "Free week already redeemed" state if they try again

CLIENT FILES THAT WILL CONSUME THIS
-----------------------------------
- src/context/AuthContext.jsx (mergeProfile reads it)
- src/components/pages/Account/SettingsPage.jsx (gates the card)

WHERE THE FIELD GETS SET
------------------------
Stripe webhook on `checkout.session.completed`:
  IF the redeemed coupon was the FREE7 code AND
     user.free_week_claimed_at IS NULL
  THEN UPDATE users SET free_week_claimed_at = NOW()

Defense-in-depth (don't rely only on the frontend hiding the card):
The /api/v1/subscriptions/redeem-promo endpoint should reject the
request with 400 { "detail": "You've already redeemed a promotional
code." } when free_week_claimed_at IS NOT NULL.

ALSO ON STRIPE'S SIDE
---------------------
Set on the FREE7 coupon:
  - max_redemptions_per_customer: 1
  - applies_to: weekly price only

This blocks the easy abuse of the same Stripe customer
re-redeeming the code.


============================================================
FEATURE 5 — ACTIVE SESSION LIMIT (5 PER USER)
============================================================

WHAT IT IS
----------
Cap each user at a maximum of 5 active sessions. When a 6th login
happens (any device, any auth flow — login, refresh, OAuth, etc.),
the oldest session is automatically revoked so the new session
takes its slot.

CURRENT STATE
-------------
The Settings page already lists active sessions (Active Sessions
section) and lets users manually revoke any session via
`userProfileService.revokeSession(id)`. There is currently no cap.
The frontend now displays the message "Max 5 active sessions per
user — signing in on a 6th device will automatically end your
oldest session" and renders sessions sorted newest-first, but the
actual enforcement must happen server-side because:
  - Sessions are minted server-side at login/refresh; the client
    can't preempt the create.
  - Two near-simultaneous logins can both think they're #5.
  - Revoking client-side after the fact races with refresh-token
    requests already in flight from older sessions.

FRONTEND FILES THAT WILL CONSUME THIS
-------------------------------------
- src/components/pages/Account/SettingsPage.jsx (Active Sessions
  section — already mentions the 5-session cap to the user)
- src/data/services/userProfileService.js → getSessions(), revokeSession()
- src/data/services/userAuthService.js     → login(), refreshToken()

WHERE TO ENFORCE
----------------
Inside the function(s) that INSERT into the `sessions` table —
typically the login handler and any social/SSO callback. After
inserting the new session row, run:

  WITH ranked AS (
    SELECT id,
           ROW_NUMBER() OVER (ORDER BY last_active_at DESC, created_at DESC) AS rn
    FROM sessions
    WHERE user_id = :user_id AND revoked_at IS NULL
  )
  UPDATE sessions
  SET revoked_at = now(),
      revoked_reason = 'session_limit_exceeded'
  WHERE id IN (SELECT id FROM ranked WHERE rn > 5);

This keeps the 5 most recently active sessions and revokes the rest
in a single statement. Run this transactionally with the new
session insert so a race can't push the count over 5.

REFRESH-TOKEN BEHAVIOR
----------------------
When a refresh-token request comes in for a session that was
revoked by the cap, return 401 with:
  { "detail": "Session ended — too many active devices. Please sign in again." }
The frontend's AuthContext will catch the 401 from refreshToken,
clear local state, and redirect to /account.

OPTIONAL UX TOUCH
-----------------
On a successful login that triggered a cap-eviction, include a
small flag in the response:
  {
    "access_token": "...",
    "refresh_token": "...",
    "expires_in": 3600,
    "evicted_session": {
      "device_info": "Chrome on Mac",
      "last_active_at": "2026-04-22T11:14:00Z"
    }
  }
The frontend can display a toast: "Signed out your oldest session
(Chrome on Mac) — you've reached the 5-device limit."
(Not required for v1; the silent eviction is fine.)

CONFIGURATION
-------------
Make the cap a constant in the backend (e.g. SESSION_LIMIT = 5).
Future tier-based tweaks (e.g. premium users get 10) become a
one-line change.


============================================================
FEATURE 6 — SCOUT AI CHAT (PREDICTIONS TAB)
============================================================

WHAT IT IS
----------
A premium-only conversational interface in the Predictions section
("/predictions/scout-ai") where users ask Scout AI about tonight's
slate — best picks of the day, parlay suggestions, value plays,
top strikeout edges, etc. — and get a natural-language answer
grounded in our live prediction data and odds.

CURRENT STATE
-------------
Frontend is fully built — chat UI, suggested-prompt chips,
typing indicator, message history, premium auth gate, mobile
responsive. Mock responses are generated by `mockResponseFor(prompt)`
in ScoutAIChat.jsx using a simple keyword router that returns
hardcoded text after a 1.1s setTimeout.

FRONTEND FILES THAT WILL CONSUME THIS API
-----------------------------------------
- src/components/pages/predictions/ScoutAIChat.jsx
  → swap mockResponseFor() for a real fetch (ideally streaming)
- src/components/pages/predictions/PredictionsNav.jsx
  → already includes the Scout AI tab

WHY A SERVER PROXY (vs frontend calling the LLM directly)
---------------------------------------------------------
- Frontend can't call Anthropic/OpenAI directly without leaking the API key.
- Server has the prediction data, odds, and Scout AI scouting reports
  needed to ground the LLM's answer in actual numbers.
- Per-user rate limiting and usage tracking live server-side.

ENDPOINT
--------
POST /api/v1/predictions/scout-ai/chat
Auth: premium users only (403 otherwise)
Body:
  {
    "messages": [
      { "role": "user",      "content": "What's the best pick of the day?" },
      { "role": "assistant", "content": "<previous answer>" },
      { "role": "user",      "content": "Tell me more about Skubal" }
    ]
  }
Response 200 (non-streaming v1):
  {
    "reply": "Based on tonight's slate ... <markdown-lite text>",
    "citations": [                       // optional but recommended
      { "type": "pitcher_prop", "game_pk": 123, "label": "Skubal Over 6.5 K" },
      { "type": "game",         "game_pk": 123, "label": "DET vs PIT" }
    ]
  }
Errors:
  401 { "detail": "Sign in required." }
  403 { "detail": "Scout AI is a premium feature." }
  429 { "detail": "Daily Scout AI limit reached. Try again tomorrow." }
  502 { "detail": "AI provider error. Please try again." }

STREAMING (v1.5)
----------------
For better UX, return a Server-Sent Events stream:
  Content-Type: text/event-stream
  data: { "delta": "Based on tonight's slate, here's my top pick:\n\n" }
  data: { "delta": "**Tarik Skubal — Over 6.5 K's**" }
  data: { "done": true }
The frontend already renders messages incrementally — adapting to
streaming is a small change in send().

CONTEXT THE SERVER NEEDS TO INJECT INTO THE PROMPT
--------------------------------------------------
Before each LLM call, fetch and embed:
  1. Today's game slate (game_pk, teams, time, status)
  2. Top game predictions (ML / RL / Total picks, model_prob, line_price, EV)
  3. Top pitcher props (best EV plays for tonight)
  4. Top batter props (best EV plays for tonight)
  5. Scout AI scouting report headlines for each game (already in DB)

A reasonable system prompt:
  "You are Scout AI, the in-app assistant for Sandlot Picks.
   You only answer questions about tonight's MLB slate using the
   data provided in the context. Be concise, opinionated, and
   data-grounded. When you cite a play, include the line, the
   model probability, and the EV. Never invent picks or numbers.
   Refuse off-topic questions politely."

PROVIDER RECOMMENDATION
-----------------------
Anthropic Claude — claude-sonnet-4-6 for quality, claude-haiku-4-5
if cost is a concern. Stream the response for snappy UX.

USAGE LIMITS (suggested)
------------------------
- Premium weekly: 30 messages/day
- Premium monthly: 100 messages/day
- Premium annual: 200 messages/day
- VIP/gifted: 200 messages/day
On hitting the cap, return 429 with the detail string above.

LOGGING
-------
Persist every chat turn (user_id, prompt, response, tokens, cost,
created_at) in a `scout_ai_chats` table. Useful for evals,
debugging, and detecting abuse.


============================================================
SHARED CONVENTIONS / GOTCHAS
============================================================

ERROR FORMAT
------------
All 4xx responses must be: { "detail": "<human readable string>" }
The frontend's apiService maps `response.detail` onto `err.message`,
so anything you put in `detail` shows up in the UI.

ADMIN AUTH CHECK
----------------
The frontend reads `user.is_admin` from the JWT and from
/api/v1/users/me. Server should reject all admin-only endpoints
with 403 { "detail": "Admin access required." } when the caller
is not flagged as admin.

EMAIL OPT-OUT
-------------
For both Alerts (modal/general) and Campaigns, ALWAYS exclude
users with email_updates = FALSE if the alert/campaign would
otherwise reach them by email. (Modal alerts are in-app only,
so they're exempt from that filter — but general inbox alerts
that ALSO trigger an email notification, if you add that, must
respect the flag.)

DATE/TIME FORMAT
----------------
All timestamps in responses should be ISO-8601 UTC with a 'Z'
suffix. Frontend parses with `new Date(iso)`.

PAGINATION
----------
For list endpoints (alerts, campaigns) v1 can return last 50–100
records inline. Add cursor pagination later if any list grows
beyond a few hundred entries.

============================================================
FEATURE 7 — HERO IMAGE UPLOAD (FASTAPI PROXY → SUPABASE STORAGE)
============================================================

WHAT IT IS
----------
The article/blog editor's "Hero Image URL" field is currently a
URL-only text input. Admins want to upload an image directly from
their machine and have the resulting public URL filled in for them.

CURRENT STATE
-------------
- The Supabase Storage bucket `post-images` has been created
  (public reads enabled, 5 MB limit, allowed MIME types png/jpeg/
  webp/gif). No RLS policies are needed — see auth context below.
- Editor field at src/components/pages/Admin/ArticleEditor.jsx:837
  is a <input type="url"> that stores whatever the admin pastes.
- No Storage usage exists anywhere in src/ today.

AUTH CONTEXT (IMPORTANT — DRIVES THE DESIGN)
--------------------------------------------
The application uses **FastAPI-issued JWTs for auth**, not Supabase
Auth. The frontend Supabase client is created with the anon key
(public, in the bundle). `public.users.id` is an integer; nothing
exists in the database that ties a Supabase Auth UUID to an app
user. Consequences:

  - `auth.uid()` from RLS policies is always null on browser calls.
  - RLS policies attempting to gate by app-user identity will lock
    out everyone, not just non-admins.
  - The browser CANNOT be trusted to authorize its own uploads.

Therefore the upload MUST be proxied through the FastAPI server,
which already has admin verification (`get_current_admin_user`)
and the Supabase service_role key (already in .env — your existing
scripts/syncContentFromSupabase.js uses SUPABASE_SERVICE_ROLE_KEY).
The service_role key bypasses Storage RLS entirely, so the bucket
needs no extra policies beyond the implicit public-read behavior.

WHAT NEEDS TO BE DONE — A SINGLE FASTAPI ENDPOINT
-------------------------------------------------

(1) NEW ENDPOINT
    POST /api/v1/admin/upload
    Auth: admin only (use existing get_current_admin_user)
    Content-Type: multipart/form-data
    Form fields:
      file:   the binary image (required)
      kind:   "hero" | "block"  (optional, default "hero" — drives
                                 the storage path prefix)
      slug:   string (optional — used in the storage path for
                      human-readable layout, falls back to "misc")

    Behavior:
      a. Validate Content-Type starts with "image/" and is in:
           ["image/png", "image/jpeg", "image/webp", "image/gif"]
         else 400 { "detail": "Only PNG, JPEG, WebP, or GIF images
                              are allowed." }
      b. Validate file size <= 5 MB (5_242_880 bytes)
         else 400 { "detail": "Image must be 5 MB or smaller." }
      c. Build the storage path:
           {kind}/{slug or "misc"}/{epoch_ms}-{slugified_filename}
         where slugified_filename strips spaces/special chars and
         keeps the original extension. Example:
           hero/welcome-2026-season/1715800000000-banner.png
      d. Upload via Supabase Python client:
           supabase.storage
             .from_("post-images")
             .upload(path, file_bytes, file_options={"content-type": mime})
      e. Construct the public URL:
           {SUPABASE_URL}/storage/v1/object/public/post-images/{path}
         (or use supabase.storage.from_("post-images").get_public_url(path))

    Response 201:
      {
        "url": "https://xyz.supabase.co/storage/v1/object/public/post-images/hero/welcome-2026-season/1715800000000-banner.png",
        "path": "hero/welcome-2026-season/1715800000000-banner.png",
        "size_bytes": 184231,
        "content_type": "image/png"
      }

    Errors:
      400 { "detail": "<specific reason>" }
      403 { "detail": "Admin access required." }
      502 { "detail": "Storage upload failed: <provider message>" }

(2) NO DATABASE CHANGES
    The returned URL is written into the existing
    content_posts.hero_image_url column (text). No schema change.

(3) NO RLS POLICIES NEEDED
    Reads: bucket is already public.
    Writes: only the FastAPI server (with service_role) can write.
            Browser-side anon-key uploads to the bucket will be
            implicitly denied because no INSERT policy exists.
    This is the desired security posture for this stack.

(4) OPTIONAL — DELETE ENDPOINT (NOT REQUIRED FOR V1)
    Once content rotation is happening, an admin DELETE endpoint
    would let stale images be cleaned up:
      DELETE /api/v1/admin/upload  body { "path": "<storage path>" }
    Skip for v1; orphaned images can be cleaned up by a future
    sweep job that compares storage objects to content_posts.hero_image_url
    references.

FRONTEND CHANGES (HANDLED IN A FOLLOW-UP CHANGE, NO ACTION NEEDED)
------------------------------------------------------------------
Once the endpoint is live, the editor will:
  a. Add a small "Upload image" button next to the existing URL input.
  b. File picker (accept="image/*", 5MB max) → POST multipart to
     /api/v1/admin/upload with form fields { file, kind: "hero",
     slug: post.slug }.
  c. On success, set hero_image_url to response.url.
  d. Show inline upload progress + error states.
The existing URL input stays — admins can still paste an external URL.

CONFIRMATION TO SEND BACK
-------------------------
Reply with:
  - Endpoint deployed and reachable at /api/v1/admin/upload
  - Confirmation of the multipart contract above
  - The exact public URL pattern the endpoint returns (so the FE
    knows to expect the storage.../object/public/... form)
Once confirmed, the FE upload UI will be wired in a single follow-up.


============================================================
FEATURE 8 — MIGRATE content_posts TO USER DB (CMS BEHIND FASTAPI)
============================================================

WHAT IT IS
----------
Today the article/blog CMS table `content_posts` lives in the
Supabase project and is read/written from the browser directly via
the Supabase JS client using the anon key. The user wants this
table moved into the existing user_directory Postgres DB and put
behind the FastAPI admin auth layer — same pattern used for
alerts, campaigns, and AI usage logs.

WHY (BUSINESS + SECURITY MOTIVATION)
------------------------------------
1. **Security**: The anon key ships in the public bundle. Any
   visitor with devtools can call `supabase.from('content_posts')
   .insert/.update/.delete(...)`. Whether they succeed depends on
   whatever RLS policies (if any) exist on the table — server
   agent should audit current policies during the migration. Moving
   the table behind FastAPI's `get_current_admin_user` closes the
   path entirely and aligns content with how alerts/campaigns are
   already gated.
2. **Architectural consistency**: User has stated their preference
   is "two databases — MLB data + user data, with all user-touching
   tables in the user DB." `content_posts` belongs in the user DB
   per that mental model.
3. **Foundation for future features**: Once behind FastAPI, future
   features (audit logging on edits, server-side content validation,
   webhook triggers on publish, draft autosave throttling, etc.)
   become trivial to add.

CURRENT STATE
-------------
- Frontend uses `supabase.from('content_posts')` in two places:
  - src/components/pages/Admin/AdminPage.jsx (list + delete)
  - src/components/pages/Admin/ArticleEditor.jsx (load + insert + update)
- Build-time sync at scripts/syncContentFromSupabase.js pulls every
  published row using SUPABASE_SERVICE_ROLE_KEY and writes the
  static JSON files the public site reads (article.json,
  moreArticles.json, blogs.json). react-snap pre-renders the
  bundled site at deploy time — this SEO architecture must be
  preserved.
- Hero image upload (Feature 7) is already correctly behind
  FastAPI. It writes a Storage object and returns a URL string;
  that URL goes into content_posts.hero_image_url. No change to
  hero upload as part of this migration.

WHAT THE SERVER AGENT NEEDS TO DO
---------------------------------

(1) AUDIT CURRENT SUPABASE RLS ON content_posts (PRE-MIGRATION)
    Quickly check what policies exist today and what's actually
    writable from the anon key. The frontend writes succeed today,
    so either RLS is disabled or there's a permissive policy.
    Document what you find so the security baseline is captured.
    SQL:
      SELECT polname, polcmd, polroles, polqual, polwithcheck
      FROM pg_policy
      WHERE polrelid = 'public.content_posts'::regclass;

      SELECT relrowsecurity, relforcerowsecurity
      FROM pg_class
      WHERE relname = 'content_posts' AND relnamespace = 'public'::regnamespace;

(2) CREATE THE TABLE IN user_directory DB (ALEMBIC MIGRATION)
    Mirror the existing Supabase schema exactly. Best source of
    truth is the columns the frontend currently reads/writes:
      id                    serial primary key
      type                  text not null            -- 'article' | 'blog'
      title                 text not null
      slug                  text not null unique
      author                text not null
      status                text not null default 'draft'  -- 'draft' | 'published'
      date                  date null
      tags                  text[] not null default '{}'
      summary               text default ''
      hero_image_url        text null
      hero_image_alt        text null
      content               jsonb not null default '[]'    -- block array
      read_time_minutes     int null
      estimated_word_count  int null
      seo                   jsonb not null default '{}'
      affiliate_cta         jsonb not null default '{"enabled":false}'
      affiliate_disclaimer  text null
      related_posts         text[] not null default '{}'
      reference_urls        text[] not null default '{}'
      created_at            timestamptz not null default now()
      updated_at            timestamptz not null default now()
      created_by            int null references users(id) on delete set null

    Indexes:
      content_posts (status, type, date DESC)   -- list filtering
      content_posts (slug)                       -- public lookup
      content_posts (created_at DESC)            -- admin recent-list

(3) ONE-TIME DATA MIGRATION SCRIPT
    Pull every row from the existing Supabase content_posts table
    and insert into the new user_directory table. Preserve ids if
    feasible (re-sequence the primary key afterward so future
    inserts don't collide).

    The frontend currently fetches with .select(
      'id, type, title, slug, status, date, tags, author, updated_at'
    ) for the list, and a full row for the editor. So both shapes
    need to round-trip.

    Sanity check after migration: counts match, no NULL on NOT NULL
    columns, all published rows still resolvable by slug.

(4) NEW FASTAPI ROUTER — content_posts_router.py
    Auth model:
      - Admin-only:    list (all statuses), create, update, delete, get-by-id
      - Public/anon:   read-by-slug (for the build-time sync script
                       and any future runtime fetches)

    Endpoints:

    (a) GET /api/v1/admin/content-posts
        Auth: admin
        Query params: type ('article' | 'blog' | null),
                      status ('draft' | 'published' | null),
                      q (title substring, null)
        Returns: [{ id, type, title, slug, status, date, tags,
                    author, updated_at }] (lightweight list shape)

    (b) GET /api/v1/admin/content-posts/{id}
        Auth: admin
        Returns: full row including content/seo/affiliate_cta blobs

    (c) POST /api/v1/admin/content-posts
        Auth: admin
        Body: full row payload (title, slug, type, ... — see schema)
        Behavior: insert, set created_by = current_admin.id
        Response 201: created row (full shape)
        Errors: 400 on missing required fields, 409 if slug exists

    (d) PUT /api/v1/admin/content-posts/{id}
        Auth: admin
        Body: full row payload (allow partial via Pydantic Optional)
        Behavior: update + bump updated_at
        Response: updated row (full shape)
        Errors: 404 if not found, 409 if slug change collides

    (e) DELETE /api/v1/admin/content-posts/{id}
        Auth: admin
        Response: 204

    (f) GET /api/v1/content-posts (PUBLIC)
        No auth.
        Query: type ('article' | 'blog' | null), limit (default 50)
        Returns ONLY status='published' rows, sorted by date DESC.
        Used by the sync script — replaces the current Supabase
        service-role fetch.
        Optional: separate endpoint for slug lookup
        GET /api/v1/content-posts/{slug} returns single full row.

    Validation/notes:
      - slug must match /^[a-z0-9-]+$/
      - type must be in ('article','blog')
      - status must be in ('draft','published')
      - tags + related_posts + reference_urls accept JSON array bodies
        and store as text[]

(5) DROP THE SUPABASE TABLE AFTER VERIFICATION
    Only after the FE swap (handled separately) is deployed and
    confirmed reading/writing successfully against the new
    endpoints. Suggested holding period: 7 days minimum.

CONFIRMATION TO SEND BACK
-------------------------
Reply with:
  - Migration ran successfully, N rows copied
  - All six endpoints deployed and reachable
  - RLS audit result on the old Supabase table (separate finding)
  - Any deviations from the schema above (column types/nulls)
Once confirmed, the frontend will be wired in a single follow-up:
  - New contentService.js (mirrors alertsService pattern)
  - Replace all `supabase.from('content_posts')...` calls in
    AdminPage.jsx + ArticleEditor.jsx
  - Repoint scripts/syncContentFromSupabase.js to the new public
    endpoint (rename to syncContentFromApi.js)
  - Remove src/lib/supabaseClient.js if nothing else uses it


============================================================
FEATURE 9 — AI ASSIST: AUTO-FILL SEO FIELDS FROM CONTENT
============================================================

WHAT IT IS
----------
A single "✨ Auto-fill SEO from content" button in the SEO card
of the article/blog editor that calls the AI proxy and populates
THREE fields at once:
  - seo_title          (SEO Title / title_tag — ~50-60 chars)
  - seo_description    (Meta description — ~150-160 chars)
  - seo_keywords       (5-10 keyword phrases, comma-joined on FE)

Unlike the existing per-field AI helpers (article_title returns 5
suggestions, article_summary returns one paragraph), this is a
single bulk-fill action: one click → three populated fields.

CURRENT STATE
-------------
- The SEO card at src/components/pages/Admin/ArticleEditor.jsx has
  three plain inputs (seo_title, seo_description, seo_keywords)
  with NO AI helpers today.
- The existing AI proxy task surface (Feature 3 task contract)
  has no task that produces SEO fields. Adding one new task is the
  minimum change.
- All other AI tasks already work fine; this extends without
  breaking any of them.

WHAT NEEDS TO BE DONE — ONE NEW AI TASK
---------------------------------------

(1) ADD TASK: article_seo
    Endpoint: POST /api/v1/admin/ai/generate   (existing, no URL change)
    Auth: admin only (existing dependency)

    Request body:
      {
        "task": "article_seo",
        "context": {
          "title":         "<post.title — required, can be empty>",
          "summary":       "<post.summary — required, can be empty>",
          "content_text":  "<flattened body blocks — required, can be empty>",
          "type":          "article" | "blog"
        }
      }

    Validation:
      - If title + summary + content_text are all empty strings,
        return 400 { "detail": "Need a title, summary, or body content
        to generate SEO fields from." }
      - Otherwise proceed with whatever's provided.

    Response shape — NEW (structured, not text/texts):
      200 {
        "seo": {
          "title_tag":        "string (≤60 chars, ≥10 chars)",
          "meta_description": "string (≤160 chars, ≥50 chars)",
          "keywords":         ["string", ...]   // 5-10 entries, lowercase
        }
      }

    Why a new top-level key (`seo` instead of `text`/`texts`)?
    Because the response is a structured object with multiple
    fields, not a single string or an array of strings. The FE
    aiService already type-checks the response shape and routes
    accordingly — adding `seo` is a one-line check.

    Errors:
      400 { "detail": "Need a title, summary, or body content
                       to generate SEO fields from." }
      403 { "detail": "Admin access required." }
      429 { "detail": "AI usage limit reached. Try again in 1 hour." }
      502 { "detail": "AI provider error." }

(2) PROMPT GUIDANCE FOR THE LLM
    Brand voice: punchy, data-grounded, baseball-fluent, no hype
    (same system prompt as the other article tasks).
    Add to the task-specific prompt:
      - title_tag should mirror the post title's intent but be
        optimized for search — concise, front-loaded with the
        primary keyword, ≤60 chars so it doesn't truncate in SERPs.
      - meta_description should read like a SERP snippet that
        earns the click: ≤160 chars, includes the post's
        primary keyword + a clear value proposition.
      - keywords should be 5-10 short noun phrases (lowercase,
        comma-separated when joined for display). Prioritize:
          * the player/team/concept named in the post
          * the post's actionable angle ("pitcher props",
            "skubal strikeout edge", etc.)
          * the publication's domain ("mlb predictions",
            "baseball analytics", etc.)
        Avoid generic stop-words ("the best", "for fans"), avoid
        duplicates, lowercase only.
      - All three fields should reference the SAME post — don't
        wander off into related-but-different topics.

(3) MODEL CHOICE
    claude-haiku-4-5 is plenty for this — structured generation
    from short inputs, no creative leap needed. Use the cheaper
    model unless quality lags in practice.

FRONTEND CHANGES (HANDLED IN A FOLLOW-UP, NO ACTION NEEDED)
-----------------------------------------------------------
Once the task is live, the editor will:
  a. Add a small "✨ Auto-fill SEO from content" button at the
     top of the SEO card.
  b. Click → call aiGenerate('article_seo', { title, summary,
     content_text, type }).
  c. Service handler routes the new `seo` response shape and
     returns { title_tag, meta_description, keywords }.
  d. FE populates seo_title, seo_description, and seo_keywords
     (joined with ", ") in one setPost call.
  e. Inline status: "Generating…" → "✓ Filled" → silent.
  f. Guard: button disabled when title + summary + body are all
     empty (mirrors the Summary AI button guard from earlier).
  g. Errors render in the existing AI modal error pattern (or a
     small inline banner here since there's no modal needed —
     this is a direct-action button, not a multi-step picker).

CONFIRMATION TO SEND BACK
-------------------------
Reply with:
  - Task `article_seo` accepted on /admin/ai/generate, response
    shape confirmed as `{ seo: { title_tag, meta_description,
    keywords } }`.
  - Sample successful payload + response so FE can validate the
    parser.
  - Confirmation the empty-inputs 400 fires correctly.
Once confirmed, the FE will be wired in a single follow-up.


============================================================
FEATURE 10 — CAMPAIGN DETAIL (FULL EMAIL VIEW IN ADMIN)
============================================================

WHAT IT IS
----------
A side drawer in the Campaign Manager (admin dashboard) that opens
when an admin clicks a campaign row, showing the full email exactly
as it was sent: subject, body (rendered as markdown using the same
pipeline the server already uses for outbound emails), CTA button,
audience, recipient count, sent timestamp, status.

Same UX pattern as the Alert read-receipts drawer (Feature 1A) and
the Article preview drawer in the editor — slide-in side panel,
Esc-to-close, click-overlay-to-close.

CURRENT STATE
-------------
- GET /api/v1/admin/campaigns returns a LIGHTWEIGHT list shape:
    { id, subject, audience, recipient_count, sent_at, status }
  No body, no CTA, no created_at — by design, since the list view
  doesn't need them and they can be heavy (long body strings).
- No GET /api/v1/admin/campaigns/{id} endpoint exists today.
- The frontend Campaign Manager rows are currently <Link> elements
  that navigate to /admin/campaigns (the composer page). The
  requested behavior is: row click opens a drawer showing the full
  email; the "Manage campaigns" link in the section header still
  navigates to the composer.

WHAT NEEDS TO BE DONE — ONE NEW ENDPOINT
----------------------------------------

(1) NEW ENDPOINT
    GET /api/v1/admin/campaigns/{campaign_id}
    Auth: admin only (use existing get_current_admin_user)

    Response 200:
      {
        "id":              <int>,
        "subject":         <string>,
        "body":            <string — original markdown the admin
                                     typed in the composer, BEFORE
                                     server-side _render_markdown_body
                                     transformation>,
        "cta_label":       <string | null>,
        "cta_url":         <string | null>,
        "audience":        "all" | "free" | "premium" | "weekly" |
                            "monthly" | "annual" | "lapsed",
        "recipient_count": <int>,
        "status":          "sent" | "failed" | "draft",
        "sent_at":         <ISO-8601 datetime with Z | null>,
        "created_at":      <ISO-8601 datetime with Z>,
        "created_by":      <int | null>
      }

    Errors:
      403 { "detail": "Admin access required." }
      404 { "detail": "Campaign not found." }

    Why return the raw markdown body, not the rendered HTML?
      - The frontend already has react-markdown wired in
        CampaignsPage.jsx's EmailPreview component with inline
        styles that mirror the server's _render_markdown_body
        output (see Feature 2 conventions on email template).
      - Reusing that same component for the drawer means preview
        and detail-view render identically — single source of truth
        for "how does this look to the recipient."
      - Sending raw markdown keeps the payload smaller than rendered
        HTML with inline styles.

(2) NO SCHEMA OR RLS CHANGES
    The campaigns table already stores body + cta_label + cta_url +
    created_at + created_by. This endpoint is a pure read against
    existing columns.

FRONTEND CHANGES (HANDLED IN A FOLLOW-UP, NO ACTION NEEDED)
-----------------------------------------------------------
Once the endpoint is live, the admin dashboard will:

  a. Add campaignsService.getCampaign(id) hitting the new endpoint.
  b. Convert the Campaign Manager rows from <Link to="/admin/campaigns">
     to clickable rows that open a side drawer (similar to the
     existing Alert reads drawer at AdminPage.jsx).
  c. Drawer fetches the full campaign on open, then renders:
       - Subject as the drawer header
       - Audience pill + recipient count + "Sent · {timestamp}"
       - The same EmailPreview component already used in the
         composer — passed the fetched { subject, body, cta }
       - Loading state while the fetch is in flight
       - Inline error banner if the fetch fails (404, 403, etc.)
  d. Esc + click-overlay close the drawer; keyboard accessible.
  e. The "Manage campaigns →" header link still navigates to the
     composer page for editing/sending new campaigns.

CONFIRMATION TO SEND BACK
-------------------------
Reply with:
  - Endpoint live at GET /api/v1/admin/campaigns/{id}
  - Confirmation that body is returned as the raw markdown string
    the admin typed (not the rendered HTML)
  - 404 detail string matches "Campaign not found." exactly so the
    FE error banner is consistent with other endpoints
Once confirmed, the FE drawer wires up in a single follow-up.


============================================================
FEATURE 11 — DELETE EMAIL CAMPAIGNS (ADMIN CLEANUP)
============================================================

WHAT IT IS
----------
Admins want to remove sent/failed/draft campaign rows from the
Campaign Manager. Today the list grows forever — no UI affordance
and no endpoint exists to prune it. Add a single DELETE endpoint
and the frontend will surface a delete button inside the campaign
detail drawer (Feature 10) with a two-step confirm flow.

This does NOT recall sent emails (which is impossible) — it just
removes the historical record from the admin dashboard.

CURRENT STATE
-------------
Endpoints today on the campaigns router:
  GET    /api/v1/admin/campaigns               (list, lightweight)
  GET    /api/v1/admin/campaigns/{id}          (full detail — Feature 10)
  GET    /api/v1/admin/campaigns/audience-counts
  POST   /api/v1/admin/campaigns/test
  POST   /api/v1/admin/campaigns/send

There is no DELETE. The frontend Campaign Manager rows in
src/components/pages/Admin/AdminPage.jsx render from the list
endpoint and currently have no destructive actions wired.

WHAT NEEDS TO BE DONE — ONE NEW ENDPOINT
----------------------------------------

(1) NEW ENDPOINT
    DELETE /api/v1/admin/campaigns/{campaign_id}
    Auth: admin only (use existing get_current_admin_user)

    Behavior:
      - Hard delete by default (row removed from campaigns table).
        Sent emails have already left the building; the row is just
        a historical record. Deleting it has no downstream side effect.
      - If a campaign_recipients tracking table was added later
        (Feature 2 listed it as optional), cascade those rows. Today
        that table doesn't exist, so this is a forward-looking note.

    Response 204 No Content on success.

    Errors:
      403 { "detail": "Admin access required." }
      404 { "detail": "Campaign not found." }      // match the GET 404 message exactly

    ROUTE ORDER REMINDER (per the bug caught in Feature 10):
    FastAPI matches routes in registration order. The dynamic
    /{campaign_id} routes (GET + DELETE) must be registered AFTER
    the static sibling routes (/audience-counts, /test, /send).
    Putting DELETE next to the existing GET /{campaign_id} is the
    natural placement and preserves the convention.

SOFT-DELETE ALTERNATIVE (DECIDE WITH PRODUCT BEFORE BUILDING)
-------------------------------------------------------------
Hard delete is recommended for v1 — campaigns are immutable historical
records once sent. If audit/compliance requirements emerge later
(e.g. "we deleted a campaign that contained a regulatory misstep,
but we need to prove we deleted it"), switch to soft-delete:
  ALTER TABLE campaigns ADD COLUMN deleted_at timestamptz NULL;
  -- And filter list/detail queries: WHERE deleted_at IS NULL
Skip this for v1 unless someone asks specifically for retention.

NO RLS / NO BUCKET WORK
-----------------------
Pure read+delete against existing campaigns table. No Storage
implications. Service-role-equivalent backend auth via the
existing admin JWT middleware.

FRONTEND CHANGES (HANDLED IN A FOLLOW-UP, NO ACTION NEEDED)
-----------------------------------------------------------
Once the endpoint is live, the admin dashboard will:

  a. Add campaignsService.deleteCampaign(id) — one-line addition,
     same shape as adminDeleteAlert / contentService.adminDelete.
  b. Add a "Delete campaign" button to the CampaignDetailDrawer
     header (below the meta row, before the email preview body).
  c. Two-step confirm pattern (same as posts table + alerts):
     "Delete" → "Confirm" / "Cancel" inline.
  d. On 204: close the drawer, remove the row from the local
     campaigns state via setCampaigns(prev => prev.filter(...)) —
     no refetch needed.
  e. On 404 (already deleted by another tab/admin): treat as a
     no-op success, close the drawer, remove from local state.
  f. On 403/other error: inline red banner with the server's detail.

CONFIRMATION TO SEND BACK
-------------------------
Reply with:
  - DELETE endpoint live at /api/v1/admin/campaigns/{id}
  - 204 on success, 404 with exact detail "Campaign not found."
  - Confirmation the route is registered AFTER the static sibling
    routes (so /audience-counts, /test, /send still resolve correctly)
  - Hard-delete vs soft-delete choice (default: hard)
Once confirmed, the FE delete button wires up in a single follow-up.


============================================================
FEATURE — BET LIBRARY / P&L TRACKER (ADMIN-ONLY)
============================================================

WHAT IT IS
----------
An admin-only "track bets you like" library in the Predictions
section. From the prop pages an admin can save a pick they've
either *taken* or on their *watchlist* (their correlation to the bet). Each
saved bet captures the Scout AI prompt/reasoning, the odds, the
model probability, EV, and confidence at pick time. The Bet
Library page then builds a flat 1-unit profit/loss + ROI record
across all tracked picks — measuring how the user does when they
follow Sandlot Picks.

Two correlation states:
  - taken     → user actually placed the bet; counts toward P/L.
  - watchlist → user was interested but didn't place the bet; tracked
                for reference, excluded from the taken P/L headline.

P/L MODEL (flat 1-unit staking)
-------------------------------
Each bet risks exactly 1 unit. On settlement:
  - won  → +unitProfit(odds), where
           unitProfit = odds>0 ? odds/100 : 100/abs(odds)
  - lost → -1
  - push →  0 (stake returned)
ROI     = (sum of unit P/L) / (wins + losses) * 100   [pushes excluded]
Win rate = wins / (wins + losses) * 100               [pushes excluded]
This math currently lives client-side in
src/data/services/betLibraryService.js (unitProfit / betPL /
summarize). The server only needs to persist the records and
their status; the FE computes the summary. (Optionally the server
can grade bets automatically — see "AUTO-GRADING" below.)

CURRENT STATE
-------------
All tracked bets live in browser localStorage under key
`spa_bet_library_v1`. This means the library does not survive a
browser/device change and can't be reconciled against real game
results. Needs a real backend, scoped per-user.

FRONTEND FILES THAT WILL CONSUME THIS API
-----------------------------------------
- src/data/services/betLibraryService.js
    (swap the localStorage read/write helpers for api.get/post/
     patch/delete — method surface already matches: list / add /
     update / remove / clear; all return Promises)
- src/components/pages/predictions/BetLibrary.jsx   (library + P/L page)
- src/data/services/matchupBets.js  (builds the Selection-field typeahead of
     trackable bets for a chosen matchup from EXISTING real endpoints —
     /predictions/today, /predictions/pitchers/{game_pk},
     /predictions/batters/{game_pk}. No new backend needed; it auto-fills
     odds/model_prob/ev onto the tracked bet at pick time.)
- src/components/pages/predictions/PredictionsNav.jsx (admin-only tab)
- (LATER) "Track" / "Watch" buttons on GameProps.jsx, PitcherProps.jsx,
     BatterProps.jsx — these will call betLibraryService.add(...)

DATA MODEL
----------
Table: tracked_bets
  id            uuid PRIMARY KEY DEFAULT gen_random_uuid()
  user_id       uuid NOT NULL REFERENCES users(id)
  correlation   text NOT NULL CHECK (correlation IN ('taken','watchlist'))
  matchup       text NOT NULL DEFAULT ''      -- "NYY @ BOS"
  market        text NOT NULL DEFAULT ''      -- "Moneyline","Total","Strikeouts"...
  selection     text NOT NULL                 -- "Boston Red Sox ML","Over 7.5"
  odds          integer NOT NULL              -- American odds at pick time
  model_prob    numeric NULL                  -- model win probability %
  ev            numeric NULL                  -- expected value %
  confidence    integer NULL CHECK (confidence BETWEEN 0 AND 5)
                                              -- Scout AI confidence; 0 = muted (user faded Scout AI's side)
  scout_prompt  text NOT NULL DEFAULT ''      -- Scout AI prompt/reasoning
  game_pk       bigint NULL                   -- link back to the game when known
  is_fade       boolean NOT NULL DEFAULT false -- bet is against Scout AI's recommended side
  grade         jsonb NULL                    -- auto-settle descriptor (market/side/line/teamId/statType/playerId)
  game_start    timestamptz NULL              -- first pitch; gates the ~2.5h settle delay
  status        text NOT NULL DEFAULT 'pending'
                CHECK (status IN ('pending','won','lost','push'))
  created_at    timestamptz NOT NULL DEFAULT now()
  settled_at    timestamptz NULL

  INDEX (user_id, created_at DESC)

ENDPOINTS (namespace: /api/v1/bet-library, admin JWT required)
--------------------------------------------------------------
  GET    /api/v1/bet-library
         → 200, array of bet records for the authenticated admin,
           newest first. Field names: camelCase to match the FE
           record shape (id, createdAt, correlation, matchup,
           market, selection, odds, modelProb, ev, confidence,
           scoutPrompt, gamePk, isFade, grade, gameStartEpoch,
           status, settledAt) — OR return snake_case (game_start as
           ISO; grade as jsonb) and we'll map in the service.
           State your choice.

  POST   /api/v1/bet-library
         body: { correlation, matchup, market, selection, odds,
                 modelProb, ev, confidence, scoutPrompt, gamePk,
                 isFade, grade, gameStartEpoch, status }
         (status optional, defaults 'pending'. isFade defaults false.
          grade is the opaque auto-settle descriptor — persist as
          jsonb, never parsed server-side unless you build the
          grading job below. gameStartEpoch is epoch ms → store as
          game_start timestamptz.)
         → 201, the created record (server sets id/createdAt).

  PATCH  /api/v1/bet-library/{id}
         body: partial — primarily { status }. When status moves to a
         terminal value ('won'|'lost'|'push') and settled_at is null,
         server stamps settled_at = now(); when status returns to
         'pending', clear settled_at.
         → 200, the updated record. 404 detail "Bet not found."

  DELETE /api/v1/bet-library/{id}
         → 204. 404 detail "Bet not found."

  (OPTIONAL) DELETE /api/v1/bet-library  → clears the caller's library.

OWNERSHIP / RLS
---------------
Every row is scoped to user_id = the JWT subject. All four
endpoints must filter/enforce user_id = current admin so one
admin can never read or mutate another's tracked bets. Standard
admin JWT middleware + a user_id WHERE clause (or RLS policy
USING (user_id = auth.uid())).

NON-ADMIN ACCESS GRANT  ✅ DONE (2026-06-21)
--------------------------------------------
The frontend grants Bet Library + Lab access to specific non-admin
users in addition to admins (FE allowlist: ADMIN_TOOL_USER_IDS in
src/data/constants/featureFlags.js — currently [16]).

RESOLVED server-side:
  - Bet Library: the four /api/v1/bet-library/* routes were relaxed
    from "admin only" to "admin OR user_id in {16}" in
    bet_library_router.py; per-user row scoping unchanged. Verified
    admin ✓, user 16 ✓, other non-admin → 403. Merged to main
    (commit 7c4cbcf). User 16 no longer 401s on save/load once main
    deploys. Keep this allowlist in sync with the FE
    ADMIN_TOOL_USER_IDS; both go away together when opened to everyone.
  - Lab data: /predictions/today and /predictions/pitchers/today are
    CONFIRMED PUBLIC (depend only on get_db; no auth/premium/admin
    dependency — only CORS). No grant needed. NOTE: if any future code
    assumes these are premium-gated, that gating is NOT implemented —
    they are open.

AUTO-GRADING
------------
Game markets (moneyline / run line / total) ALREADY auto-settle on
the client: betGrader.js (src/data/services/betGrader.js) pulls the
final game record from scheduleService (GET /scheduled-games/today
and /scheduled-games/prior — the same data the schedule page uses to
flip live games to Final) ~2.5h after first pitch and grades from
status + winning_team_id + home/away_runs_score. Each bet stores a
`grade` descriptor + `gameStartEpoch` to drive this.

Player props (pitcher / batter) ALSO auto-settle on the client now.
Each prop bet stores grade = { type:'pitcher_prop'|'batter_prop',
side, line, statType, playerId }. betGrader.gradePlayerProp() compares
the bet's line/side to the player's actual final stat, which the page
reads from the per-game H2H box scores already used by the live
tracker (gamesService.getHeadToHeadBatters/Pitchers scoped by game_pk
→ aggregatePlayers → liveBetTracker.getPlayerPropActual). No new
endpoint required. A prop with no resolvable stat (player DNP, data
delay, name not matched) stays 'pending' and retries / settles
manually.

OPTIONAL SERVER UPGRADE (robustness only — not required):
Client grading only runs while someone has the Bet Library open. For
authoritative settlement that doesn't depend on a viewer, a nightly
job could settle every market server-side and stamp status/settled_at:
  - game markets from the final game record (status + winning_team_id
    + home/away_runs_score), and
  - player props from per-player game-log results (the existing
    /games/pitcher and /games/batter logs carry per-game stats by
    player_id) vs the bet's line/side.
If/when the backend owns settlement, it becomes the source of truth
across all markets and the client grading can be retired.

CONFIRMATION TO SEND BACK
-------------------------
Reply with:
  - The four endpoints live under /api/v1/bet-library
  - camelCase vs snake_case response choice
  - 201 on create returning the full record (incl. id, createdAt)
  - PATCH settled_at stamping behavior confirmed
  - user_id scoping/RLS confirmed
Once confirmed, betLibraryService swaps localStorage → fetch in a
single follow-up with no page-level changes.


============================================================
FEATURE — PREDICTIONS LAB (SLATE SCREENER) ENRICHMENT
============================================================

WHAT IT IS
----------
Admin-only "Lab" tab (/predictions/lab) — a dynamic, day-trader-style
screener over today's slate. Each starting pitcher is one row enriched
with odds, the model's projection, and team/opponent form; the admin
stacks any filters to surface hidden-value spots (e.g. "30+ starter on
<=4 days rest, moderate favorite, vs a near-.500 team").

CURRENT STATE
-------------
Fully working on existing endpoints — no backend required to run:
  /predictions/today, /predictions/pitchers/today, /teams/standings.
The filter catalog is data-driven (src/data/services/labFilters.js): a
filter exists for every attribute, and several are already flagged
`available: false` because their data isn't on the payload yet. Those
filters render but are inert until the fields below land. Adding the
field to the row (src/data/services/labSlate.js) auto-activates them.

DATA REQUESTED (so the inert filters light up)
----------------------------------------------
On each starter in /predictions/pitchers/today (home_pitcher /
away_pitcher objects), add:
  age          integer   -- pitcher age in years
  days_rest    integer   -- days since the pitcher's previous start
  season_era   numeric   -- current-season ERA
  throws       text      -- 'L' | 'R'
  (optional)   recent form flags for "hot/struggling" screens

Per-team situational + recent (last-5-game) offense, on /teams/standings
rows or a lightweight slate endpoint keyed by team. No existing endpoint
exposes rolling team offense (getTeamLast10 is game records only), so
these power the opponent recent-form screens:
  risp_avg     numeric   -- team AVG with runners in scoring position
  key_injury   boolean   -- notable lineup/rotation injury flag
  ops_l5       numeric   -- team OPS over last 5 games        (mapped → oppOpsL5)
  runs_l5      numeric   -- team runs/game over last 5 games   (mapped → oppRunsL5)
  avg_l5       numeric   -- team batting AVG over last 5 games (mapped → oppAvgL5)

FRONTEND
--------
No new service needed — labSlate.js already reads these field names off
the pitcher node (age, days_rest, season_era, throws) and the opponent's
team form (rispAvg, opsL5, runsL5, avgL5). Ship the fields and the
matching Lab filters (Pitcher age, Days of rest, Season ERA, Throwing
hand, Opp AVG w/ RISP, Opp OPS/runs/AVG L5) activate with zero FE
changes. The L5 stats map: ops_l5→oppFm.opsL5, runs_l5→oppFm.runsL5,
avg_l5→oppFm.avgL5 in src/data/services/labSlate.js. Confirm field names
or tell me yours and I'll map them.


============================================================
FEATURE — SCOUT DESK: CUT PLAYS OPEN THE WAR ROOM   ✅ DONE (2026-06-27)
============================================================

RESOLVED: GET /api/v1/predictions/scout-desk now returns cut[] with the
full picks[] shape (incl. analysts[], consensus, reasoning, audit, +
the cut-specific `reason`). No migration, no new endpoint — it just
stopped trimming (live on main, commit 9eae373). FE was already built:
Honorable Mentions are now clickable and open the War Room. Note: boards
frozen before this change keep the old trimmed cut[] until regenerated;
new days get the full shape, or an admin can hit ?refresh=true to
upgrade today's board immediately.

WHAT IT WAS
-----------
The Scout Desk shows the day's top 3 pitcher-prop picks plus an
"Honorable Mentions" list — the shortlisted-but-cut plays. Each top
pick opens a "War Room" drawer with the 4 analyst personas' takes,
audit basis, matchup context, and Track/Fade actions. We want the
Honorable Mentions to open that SAME drawer, so a user who disagrees
with the top 3 can dig into the next-best options.

CURRENT STATE
-------------
The frontend is already done and will light up with zero FE changes
the moment the data arrives:
  - src/data/services/scoutDesk.js maps cut[] through the same mapPick()
    as picks[].
  - src/components/pages/predictions/ScoutDesk.jsx renders each cut row
    as a clickable button that opens the War Room — but ONLY when the
    play carries analysts[] (so we never open an empty drawer).
Right now the rows are NOT clickable because the server trims cut[]
to roughly { selection, reason } — there is no analysts[] / reasoning /
audit / consensus on them, and the client cannot reconstruct the
multi-agent reasoning (it's generated server-side).

WHAT'S NEEDED FROM THE SERVER
-----------------------------
GET /api/v1/predictions/scout-desk

Return each entry in cut[] with the SAME full play shape as picks[],
not a trimmed object. Concretely, a cut entry should include all the
fields a pick has:

  { id, gamePk, playerId, pitcherName, market, statType, side, line,
    selection, odds, modelProb, ev, projection, gap, conviction,
    audit { winPct, basePct, gapVsBase, sample, ... },
    reasoning { rationale, context { last5Summary, parkFactor, opponentForm } },
    scoutReasoning,
    analysts: [ { key, name, vote, confidence, take }, ... ],   // ← the gating field
    consensus { level, label },
    reason }                                                    // keep the cut-specific "why cut"

Notes:
  - analysts[] is the field the FE checks to enable the click, and is
    the whole point (the persona breakdown). It must be present.
  - Keep the existing `reason` string (shown under the selection).
  - Same camelCase shape and the same admin/allowlist gating as picks[]
    — these are the same plays, just ranked below the top 3.
  - No new endpoint; just stop trimming cut[] in the existing response.

CONFIRMATION TO SEND BACK
-------------------------
Reply that cut[] now returns the full picks[] shape (incl. analysts[]).
Nothing else changes on the FE — the Honorable Mentions become
clickable and open the War Room automatically.


============================================================
FEATURE — ANALYTICS LAB (CHART EXPLORER)
============================================================

WHAT IT IS
----------
A new Stats page at /analytics-explorer ("Analytics Lab", in the
Stats nav dropdown, signed-in users only). It's a semantic chart
builder: the user picks dimensions (grouping/labels), measures (the
numbers plotted), and filters (ranges, toggles, season windows)
through a friendly UI — never tables or SQL — and sees teams plotted
as scatter / horizontal-bar / line charts. Switching chart type is a
pure client re-render of the same payload (never a refetch).

Headline query the UI must support end-to-end:
  "Average height and weight of World Series-winning teams 2010-2015,
   sorted heaviest-to-lightest."

CURRENT STATE
-------------
The entire frontend is built and will light up with ZERO FE changes
the moment these two endpoints return data. Until then the page shows
a clean "Couldn't load the analytics schema" state with a retry. No
mock data, no synthetic fallbacks — it renders only real responses.

  - Client service:  src/data/services/analyticsChartService.js
      getSchema()           -> GET  /analytics/schema
      runQuery(spec)        -> POST /analytics/query
  - Page + components: src/components/pages/Stats/chart-explorer/
  - Charts: Recharts (newly added dependency).
  - All controls (filters, axis pickers, group-by, units, sliders,
    chart-type switch) are generated FROM the schema response. Nothing
    is hardcoded — add a metric server-side and it appears in the UI.

API CONVENTIONS (match existing endpoints)
------------------------------------------
Bare paths under the same host the other services use (e.g.
predictionsService calls "/predictions/today" — NOT "/api/v1/...").
JWT Bearer auth, { "detail": "<msg>" } error shape. The POST spec is a
good backend cache key — identical specs should resolve from cache.

WHAT'S NEEDED FROM THE SERVER
-----------------------------

1) GET /analytics/schema   (discovery — called once on page load)

   Returns the full registry of what's queryable so the UI can build
   itself. v1 grain is team-season (one team in one year); a future
   player grain is planned (same contract, different grain value), so
   return `grains` as a list.

   {
     "grains": ["team_season"],
     "dimensions": [
       { "field": "team",       "label": "Team",     "type": "string",
         "operators": ["eq"] },
       { "field": "season",     "label": "Season",   "type": "season",
         "operators": ["eq","between"], "min": 2000, "max": 2025 },
       { "field": "league",     "label": "League",    "type": "enum",
         "operators": ["eq"], "values": ["AL","NL"] },
       { "field": "division",   "label": "Division",  "type": "enum",
         "operators": ["eq"],
         "values": ["AL East","AL Central","AL West",
                    "NL East","NL Central","NL West"] },
       { "field": "ws_result",  "label": "WS Winner", "type": "boolean",
         "operators": ["eq"] }
     ],
     "measures": [
       { "field": "avg_height", "label": "Avg Height", "unit": "in",
         "operators": ["between","gt","lt"], "default_sort": "desc",
         "min": 70.0, "max": 76.0 },
       { "field": "avg_weight", "label": "Avg Weight", "unit": "lbs",
         "operators": ["between","gt","lt"], "default_sort": "desc",
         "min": 195, "max": 230 },
       { "field": "avg_age",    "label": "Avg Age",    "unit": "yrs",
         "operators": ["between","gt","lt"], "default_sort": "desc",
         "min": 24, "max": 32 },
       { "field": "team_avg",   "label": "Team AVG",   "unit": "",
         "operators": ["between","gt","lt"], "default_sort": "desc" },
       { "field": "team_ops",   "label": "Team OPS",   "unit": "",
         "operators": ["between","gt","lt"], "default_sort": "desc" },
       { "field": "team_era",   "label": "Team ERA",   "unit": "ERA",
         "operators": ["between","gt","lt"], "default_sort": "asc" },
       { "field": "whip",       "label": "WHIP",       "unit": "",
         "operators": ["between","gt","lt"], "default_sort": "asc" },
       { "field": "wins",       "label": "Wins",       "unit": "W",
         "operators": ["between","gt","lt"], "default_sort": "desc" },
       { "field": "run_differential","label":"Run Diff","unit": "",
         "operators": ["between","gt","lt"], "default_sort": "desc" }
     ]
   }

   Per-field contract the FE relies on:
     - field         : id used in the query spec + as the row key
     - label         : display name
     - type          : one of string | enum | boolean | season | number
                       (drives which control renders; if omitted the FE
                        infers from operators/values, but please send it)
     - unit          : "lbs","in","ERA","yrs",... or "" (drives display +
                       axis labels; unit "in" is rendered as 6'1")
     - operators     : subset of ["eq","between","gt","lt"]
     - default_sort  : "asc" | "desc" (measures)
     - values        : required for enum fields (the pickable options)
     - min / max     : numeric bounds for measures/season — used to render
                       range SLIDERS. Omit and the FE falls back to free
                       min/max number inputs, so sliders need these.

2) POST /analytics/query   (the data)

   Request body (only touched fields are sent; partial ranges are
   dropped client-side so you never get a half-built filter):

   {
     "grain": "team_season",
     "filters": [
       {"field": "ws_result", "op": "eq",      "value": true},
       {"field": "season",    "op": "between", "value": [2010, 2015]}
     ],
     "dimensions": ["team", "season"],
     "measures": ["avg_weight", "avg_height"],
     "sort": [{"field": "avg_weight", "dir": "desc"}],
     "limit": 30
   }

   Notes on the request:
     - filters[].op is always one of eq | between | gt | lt. For
       between, value is a 2-element [lo, hi] array.
     - measures = the plotted numbers (X first, then Y if present).
     - dimensions = grouping/labels for each row.
     - limit cap is 500 (FE slider maxes at 500).
     - Unknown field -> 400 with { "detail": "..." }.

   Response:

   {
     "rows": [
       {"team":"Red Sox","season":2013,"avg_height":73.4,"avg_weight":207.1}
     ],
     "meta": {
       "x": {"field":"avg_weight","label":"Avg Weight","unit":"lbs",
             "min":198,"max":224},
       "y": {"field":"avg_height","label":"Avg Height","unit":"in",
             "min":70.2,"max":75.1},
       "measures":[{"field":"avg_weight","label":"Avg Weight","unit":"lbs",
                    "default_sort":"desc"}],
       "suggested_charts":["scatter","horizontal_bar","line"],
       "row_count": 1
     }
   }

   Notes on the response the FE depends on:
     - rows are FLAT records keyed by the exact dimension/measure field
       ids that were requested.
     - meta.x / meta.y carry label + unit (axis labels) and min/max
       (axis scaling). meta.y may be omitted for single-measure queries.
     - meta.suggested_charts drives the chart-type switcher. Send the
       chart types that make sense for the payload; the FE only offers
       those and renders the first by default. Use the literal values
       "scatter", "horizontal_bar", "line".
     - meta.row_count is shown as the result count.
     - Heights come back as inches (numeric); the FE formats to 6'1".
     - age is age-in-that-season, not current age.

   Drill-in (future): each point/bar is already wired to carry its full
   row (team + season + measures) into a detail drawer. A later
   team -> roster -> player layer can hang off the same row with no
   chart changes — no endpoint needed yet, just noting the direction.

CONFIRMATION TO SEND BACK
-------------------------
Reply when GET /analytics/schema and POST /analytics/query are live
with the shapes above. The Analytics Lab page will then render real
charts with no further FE work.


============================================================
FEATURE — SCOUT DESK TRACK RECORD: OPEN THE WAR ROOM MATCHUP
============================================================

WHAT IT IS
----------
On the Scout AI Desk page, the Track Record "Daily log" calendar lists
each graded desk pick per day. Each pick row is now a button that opens
the same War Room drawer used by today's board — so a user can review the
game matchup (lineups, bullpens, park) and analyst breakdown behind any
historical pick, not just today's.

The FE is already wired: clicking a daily-log pick calls the drawer with
that pick object. The drawer degrades gracefully — it renders whatever
fields are present and hides empty sections — but to show the FULL
matchup it needs the performance endpoint to return the same rich pick
shape the board endpoint returns.

ENDPOINT AFFECTED
-----------------
GET /api/v1/predictions/scout-desk/performance[?from=&to=]
  → { summary, days: [ { date, record, unitsPnl, picks: [ ... ] } ] }

WHAT THE FE NEEDS ON EACH days[].picks[] ENTRY
----------------------------------------------
Today it carries the graded summary fields:
  selection, market, side, line, projection, actual, status, pnl

To render the War Room matchup for a graded pick, also include the SAME
fields the board's picks[] already return (persist them on the frozen
board pick and echo them back here):

  - kind          "pitcher" | "game"
  - subject       game headline for game props ("Angels @ Mariners")
  - pitcherName   for pitcher props (null for game props)
  - playerId      for pitcher props (enables the player-profile link)
  - gamePk        the MLB game PK
  - statType      "strikeouts" | "game_moneyline" | ...
  - odds, modelProb, ev, conviction
  - audit         { winPct, basePct, gapVsBase, sample, ... }
  - reasoning     { rationale, context: { ... } }
        pitcher context: last5Summary, parkFactor, opponentForm,
                         matchup, pitcherProfile, restSplits, ...
        game context:    gameMatchup { home, away,
                         lineups:{home,away}, bullpens:{home,away},
                         park, form }, parkFactor
  - analysts[]    same shape as the board (key,name,vote,confidence,take)
  - consensus     { level, label }

NOTES
-----
  - These are the exact fields the board's picks already produce — the
    ask is to STORE them on the frozen pick and return them here, not to
    recompute anything. If a legacy graded pick predates this, omit the
    fields; the drawer will just show the header + result and skip the
    empty matchup/analyst sections (no FE change needed).
  - status is already returned; the FE uses it to hide the track/fade
    actions on settled picks (shows the settled result instead).

CONFIRMATION TO SEND BACK
-------------------------
Reply when days[].picks[] carries the fields above. The daily-log →
War Room matchup drawer will then render the full breakdown with no
further FE work.


============================================================
FEATURE — BETSLIP DEEP LINKS ON SCOUT AI GAME PICKS
============================================================

WHAT IT IS
----------
Users pick a preferred sportsbook in settings; each Scout AI game pick
(Moneyline / Run Line / Total) then shows a one-tap "Add to {book} slip"
button that opens their book with that exact bet pre-loaded. The FE for
this is BUILT and fails safe — but it needs deep_links on the predictions
payload to ever render a button.

CURRENT STATE (verified against production 2026-07-18)
------------------------------------------------------
The FE is fully wired and deployed-ready:
  - Preferred Sportsbook setting persists via PATCH/GET /api/v1/users/me
    (preferred_book: string|null) — endpoint confirmed live, auth-gated.
  - src/lib/betslipLinks.js resolves pick.deep_links[preferred_book],
    sanitizing {state} (user state code, lowercase) and {stake}/{wager}/
    {amount} (→ empty); any unresolved {...} → button hidden, no error.
  - ScoutAiModal renders the button on ML/RL/Total picks only.

THE BLOCKER: the deployed predictions payload is NOT returning deep_links.
A live check of all four endpoints found 0 of 45 picks carrying the field:

  GET /predictions/today            15 games / 45 picks / 0 with deep_links
  GET /predictions/moneyline/today  0 with deep_links
  GET /predictions/spread/today     0 with deep_links
  GET /predictions/totals/today     0 with deep_links

best_odds / best_odds_book ARE populated on these same picks, so odds data
exists — only the deep_links map is absent. As a result the button renders
for zero users today (every pick hits the graceful "no link" fallback).

ENDPOINT AFFECTED
-----------------
GET /predictions/today
GET /predictions/moneyline/today
GET /predictions/spread/today
GET /predictions/totals/today
  → each game's scout_ai.picks[] entry, for ML / Run Line / Total picks

WHAT THE FE NEEDS ON EACH scout_ai.picks[] ENTRY (ML/RL/Total only)
-------------------------------------------------------------------
Add a deep_links object mapping bookmaker key → betslip URL for that
pick's exact side:

  "deep_links": {
    "draftkings":     "https://sportsbook.draftkings.com/?outcomes=...",
    "fanduel":        "https://sportsbook.fanduel.com/addToBetslip?...",
    "betmgm":         "https://sports.{state}.betmgm.com/en/sports?...",
    "williamhill_us": "https://sportsbook.caesars.com/us/{state}/bet/...",
    "betrivers":      "https://...",
    "fanatics":       "https://...",
    "hardrockbet":    "https://app.hardrock.bet/?deep_link_value=..."
  }

  - Bookmaker keys must match the supported set exactly (note Caesars'
    key is williamhill_us). A book may be omitted if it has no link for
    that game — the FE just hides the button for that book.
  - {state} placeholders (Caesars/BetMGM) are passed through raw — the
    predictions endpoints are public/cached with no per-user context, so
    they can't inject state. RESOLVED APPROACH: a user.state field (see
    below). The FE substitutes it; links still containing {state} (no
    state set) are hidden, no error.
  - {stake}/{wager}/{amount} placeholders are stripped to empty by the FE.
  - Do NOT add deep_links to pitcher/batter prop picks — game props only.

SECOND ASK — user.state FIELD (unblocks Caesars + BetMGM)
---------------------------------------------------------
Add a `state` field to the user profile, mirroring how preferred_book
was added:
  - GET  /api/v1/users/me  → include  state: string | null  (2-letter,
    e.g. "NJ"; case-insensitive — FE lowercases for the URL)
  - PATCH /api/v1/users/me → accept { "state": "NJ" } to set,
    { "state": null } to clear.
FE side is ALREADY BUILT (branch spa-team-dash): a "State" select sits
next to Preferred Sportsbook, reads user.state, PATCHes it, and
betslipLinks.js substitutes it into {state}. It lights up the moment
GET /users/me returns the field.

THIRD ASK — deep_links ON BEST GAME PROPS (War Room drawer)
-----------------------------------------------------------
GET /predictions/today/best-game-props  →  { ..., picks: [ ... ] }

The "Best Game Props" board on the Game Predictions page opens a War Room
side drawer per pick. We want the same "Add to {book} slip" button in
that drawer. These picks are the Scout-Desk shape (gamePk, market,
statType, side, selection, odds, ...) — a DIFFERENT payload from
scout_ai.picks — and currently carry NO deep_links (verified live: 0 of 3).

Add the same deep_links map to each GAME pick in picks[] (kind === 'game'
/ statType in game_moneyline | game_total | game_spread). Critically, the
URL must be for THAT pick's exact side (the deep link is side-specific —
best-game-props can pick a different side than scout_ai.picks for the same
game, so the FE must NOT reuse scout_ai's links; attach the correct-side
link here server-side). Same bookmaker keys, same {state}/{stake} rules as
above. Do NOT attach to pitcher props (kind === 'pitcher').

FE side is ALREADY BUILT (branch spa-team-dash): the War Room drawer
(ScoutWarRoom.jsx) reads play.deep_links via resolveBetslipUrl and renders
the button; absent today, so it simply doesn't show. Lights up the moment
best-game-props picks[] carry deep_links.

NOTES
-----
  - No FE change is needed for the base feature when deep_links lands —
    DraftKings/FanDuel/HardRock (clean URLs) render immediately for any
    user with preferred_book set.
  - Caesars/BetMGM need BOTH deep_links (this feature) AND user.state
    (second ask) before their buttons appear.
  - Task text referenced base host sandlotpicksanalytics.com; the app
    actually uses www.sandlotpicksanalytics.com (apex does not resolve).
    Not blocking, just noting.

LIVE VERIFICATION (2026-07-18) — TWO GAPS FOUND
-----------------------------------------------
Verified against live www.sandlotpicksanalytics.com after the deploy:

  A) /predictions/today  ✅ WORKING. scout_ai.picks[] carry deep_links,
     6-book maps, keys correct (Caesars = williamhill_us). DraftKings/
     FanDuel/HardRock resolve clean; BetMGM/Caesars carry {state} (need
     user.state). Button live for those on the Scout AI modal.

  B) /predictions/today/best-game-props  ❌ NO deep_links on the live
     www endpoint — 0 of 3 picks (all Moneyline), response byte-identical
     to the pre-deploy body. Those same moneyline picks DO get links on
     scout_ai.picks, so the data exists. Looks like a stale cache or the
     deploy didn't cover this route/path. Please check caching + that the
     serve-time attach runs on the PUBLIC www best-game-props path.

  C) /api/v1/predictions/scout-desk  — 403 (admin/user_id-gated), could
     not verify without creds. Per your note it uses the same shared
     helper, so likely fine; confirm on your side.

  D) BetRivers link contains an unresolved {pickType} token (full form:
     ".../coupon={pickType}|<selectionId>|{wagerAmount}"). The FE now
     strips stake-family tokens incl. {wagerAmount}, but it CANNOT guess
     {pickType} — so BetRivers stays hidden on ALL surfaces until it's
     pre-resolved server-side. Either fill {pickType} with the correct
     coupon type or drop BetRivers from the map. (Only {state} and stake
     placeholders are FE-resolvable; anything else must be pre-filled.)

RE-VERIFICATION (2026-07-19) — PUBLIC BOARD ENDPOINTS STILL DRY
---------------------------------------------------------------
After the pitcher-prop deploy, re-checked live www. Grep for the link
fields across each public payload:

  /predictions/today                deep_links: PRESENT (45 picks) ✅
  /predictions/today/best-game-props   deep_links: 0  ❌ (2nd recheck)
  /predictions/today/best-pitcher-props deep_links: 0 ❌
  /predictions/pitchers/today          deep_links/over_link/under_link: 0 ❌

Only /predictions/today reflects links. best-game-props GREW in size
(new scoutReasoning/breakEven fields) but has ZERO occurrences of the
string "deep_links" anywhere (picks, cut, shortlist). Since the game
moneyline/total picks on that board are the SAME selections that DO get
links on /predictions/today, the data exists — the board endpoints just
aren't emitting it publicly.

Most likely a CDN/HTTP cache in front of the board routes serving bodies
from before the serve-time attach, OR www routing those paths to an
instance without the new code. Please check: does the PUBLIC www
response for these three endpoints include deep_links (not just an
internal/origin check)? A cache purge on the board routes may be all
it needs. (scout-desk is 403/gated — can't verify from FE; trusting the
shared-helper claim.)

FE IS READY: the shared War Room drawer button (ScoutWarRoom.jsx) now
renders for BOTH game and pitcher picks (isGame gate removed) — it lights
up the moment these endpoints actually carry deep_links on www.

UPDATE (2026-07-19, 2nd deploy LANDED) — re-verified live www:
  - /best-game-props (B): ✅ NOW LIVE. 3/3 picks carry deep_links;
    resolve clean for DK/FD/HardRock, BetMGM/Caesars need state.
    BetRivers correctly DROPPED (sanitizer). War Room button renders.
  - /best-pitcher-props: ✅ deployed (deep_links key present) but today's
    lone pick is {} (no book posted a link) → no button, fail-safe OK.
  - /pitchers/today: deep_links present (Scout AI prop picks). BUT
    over_link/under_link = 0 occurrences — the per-sportsbook-line
    over/under links you described aren't on this payload (only the
    pick-level deep_links maps are). Not used by the drawer button, but
    flag if you intended prop-line links for the PitcherProps line table.
  - /scout-desk (C): still 403-gated — can't FE-verify; trusting shared
    helper. Origin-side confirm welcome.
  - BetRivers: ✅ absent from ALL deep_links maps on /today (0 as a key).

CONFIRMATION TO SEND BACK
-------------------------
1) scout_ai.picks[] deep_links — ✅ confirmed live and rendering.
2) Reply when GET/PATCH /api/v1/users/me carries `state` — Caesars and
   BetMGM buttons then light up for users who set both preferred_book
   and state (FE already built).
3) Reply when the PUBLIC www responses for /best-game-props,
   /best-pitcher-props, and /pitchers/today actually include deep_links
   (they don't as of 2026-07-19) — the War Room drawer + prop surfaces
   then render with no further FE work.
4) Reply when the BetRivers {pickType} token is pre-resolved (or
   BetRivers is dropped) — BetRivers then renders on all surfaces.


============================================================
FEATURE — DROP deep_links FOR FINAL GAMES (all surfaces)
============================================================

STATUS: OPTIONAL / no longer required. The FE now hides the betslip
button on final games itself — the shared War Room drawer looks up the
pick's gamePk in scheduleService.getTodayGames() (same schedule data used
to settle bets) and hides the button when isGameFinal. Covers Best Game
Props, Scout Desk, and Pitcher Props from one place. The server-side drop
below is still a nice belt-and-suspenders (and would let the client stop
the lookup), but is not needed for correctness.

WHAT / WHY
----------
The betslip "Add to {book} slip" button must not show once a game is
final — you can't add a settled game to a slip. Verified live: picks on
FINAL games STILL return a populated deep_links map (e.g. today every
game is Final yet best-game-props picks carry draftkings/fanduel/etc.
links). The FE hides the button when it can determine the game is final,
but it only can where the surface has today's games loaded to match by
gamePk (Best Game Props). The SAME shared War Room drawer also opens from
the Scout Desk board and Pitcher Props, which have no games-status list —
so the button still renders there. Rather than thread game status into
every surface (fragile: breaks on mock-data fallback / load races), fix
it once at the source.

THE ASK
-------
When attaching deep_links (the shared deep_links helper), if the pick's
game is final/over, return an EMPTY map ({}) — i.e. skip the attach.
Applies to every surface that emits deep_links:
  - /predictions/today  (scout_ai.picks[])
  - /predictions/today/best-game-props   (picks + cut)
  - /predictions/today/best-pitcher-props
  - /predictions/pitchers/today
  - /api/v1/predictions/scout-desk        (game + pitcher picks)
Use the same game-final signal the schedule/status pipeline already has
(status in Final / Game Over / Completed Early, incl. variants like
"Final/7"). Deep links attach at serve time already, so this is a serve-
time guard next to the existing attach — no schema change.

CONFIRMATION TO SEND BACK
-------------------------
Reply when deep_links is {} for final-game picks. The FE button then
disappears on every surface automatically (it already treats an empty /
absent map as "no button"), and the client-side final check becomes a
redundant backstop.


============================================================
END OF DOCUMENT
============================================================
